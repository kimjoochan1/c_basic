#include (stdio.h)

/**
 * c 언어 기본 문법
 *  1.문장 끝(;)
 *   + 세미클론(;)을 사용하여 문자 끝을 표시
 *   + 한 줄에 한문장씩 작성
 *  2.들여쓰기 
 *   + 코드의 상하관계(포함관계) 나타냄
 *  3.함수()
 *   + 반복적인 작업을 위해 작성한 코드 묶음
 *   +()안에는 함수의 입력값
 *   +(void)는 함수의 입력값 없음
 *  4.printf()함수
 *   + 출력 함수
 *   + ()안에 입력된 값을 출력
*/
int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
